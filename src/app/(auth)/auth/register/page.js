import RegisterPage from "./register";

export const metadata = {
  title: "Create Account",
  description: "Register a new account to start using the platform",
};

export default function Page() {
  return <RegisterPage />;
}