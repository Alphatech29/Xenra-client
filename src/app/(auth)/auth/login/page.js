import LoginPage from "./login";

export const metadata = {
  title: "Login",
  description: "Sign in to access your account and manage your dashboard",
};

export default function Page() {
  return <LoginPage />;
}